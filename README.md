
---
# transformers_Text_Summarizer
## 📰 Korean News Summarization

## 프로젝트 개요

한국어 뉴스 기사를 Hugging Face Transformers 기반 **BART 모델**을 활용해 자동 요약하는 프로젝트입니다.
대규모 데이터도 처리할 수 있도록 **GPU 배치 처리 최적화**를 적용했으며, 실제 포트폴리오에서는 **작은 샘플 CSV**로 결과물을 제공합니다.

---

## 🧩 프로젝트 구조

```
transformers_Text_Summarizer/
├─ data/
│  ├─ raw/
│  │  └─ train.csv           # 원본 대용량 CSV
│  └─ processed/
│     ├─ train_small.csv     # 샘플 CSV (포트폴리오용)
│     └─ summarized_small.csv # 요약 결과
├─ src/
│  ├─ run_summarization_small.py  # 작은 CSV 요약 스크립트
│  ├─ summarizer.py               # 배치 최적화 요약 함수
│  └─ small.py                     # 작은 CSV 생성 스크립트
├─ requirements.txt
├─ README.md
└─.gitignore
```

---

## 🧠 사용 기술

* **Python**: 데이터 처리, 자동화
* **Pandas**: CSV 로드 및 데이터 처리
* **TQDM**: 배치 요약 진행률 표시
* **Transformers (Hugging Face)**: BART 모델 기반 Seq2Seq 텍스트 요약
* **PyTorch**: GPU 가속
* **GPU 최적화**: 배치 처리, `torch.no_grad()` 적용, 메모리 효율 개선

---

## 💻 실행 방법

### 환경 설치

```bash
pip install -r requirements.txt
```

### 작은 CSV로 테스트 실행

```bash
python src/run_summarization_small.py
```

* `train_small.csv`를 불러와 배치 요약 수행
* 완료 후 `summarized_small.csv` 생성

### 대용량 데이터 처리 (선택 사항)

```bash
python src/run_summarization_small.py
```

> ⚠주의: 전체 데이터는 수천~만 개 기사일 경우 **GPU 사양과 데이터 크기에 따라 시간이 매우 오래 걸릴 수 있습니다**.
> 포트폴리오용으로는 작은 CSV만 사용하세요.

---

## 코드 설명

### `summarizer.py`

* **batch_summarize** 함수

  * 배치 단위로 텍스트를 GPU에서 요약
  * `torch.no_grad()` 적용 → 메모리 절약
  * Beam Search, Length Penalty로 요약 품질 보장

### `run_summarization_small.py`

* 작은 CSV 로드 후 `batch_summarize` 적용
* 요약 결과 CSV 저장
* 진행률 표시(TQDM) 포함

### `small.py`

* 전체 CSV에서 일부 샘플만 추출해 **train_small.csv** 생성
* 테스트 및 포트폴리오용으로 사용

---

##  결과

* `summarized_small.csv` 예시:

| document      | summary     |
| ------------- | ----------- |
| 한국어 뉴스 원문 텍스트 | 모델이 생성한 요약문 |
| ...           | ...         |

* 대용량 전체 데이터용 요약 코드는 제공되지만, **실제 결과물은 작은 샘플로 제한**하여 포트폴리오에 포함

---

## 느낀 점

* GPU 배치 처리와 `no_grad()` 적용으로 메모리 효율 극대화
* 대용량 뉴스 데이터는 시간과 메모리 관리가 핵심
* 실제 프로젝트에서는 작은 샘플을 통한 **실행 검증과 시연**이 현실적

---

